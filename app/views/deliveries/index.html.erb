<p id="notice"><%= notice %></p>

<h1>Список</h1>

<table>
  <thead>
    <tr>
      <th>Откуда</th>
      <th>Куда</th>
      <th>Длина</th>
      <th>Ширина</th>
      <th>Высота</th>
      <th>Общий объем</th>
      <th>Общий вес</th>
      <th>Расстояние</th>
      <th>Стоимость</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @deliveries.each do |delivery| %>
      <tr>
        <td><%= delivery.from %></td>
        <td><%= delivery.where %></td>
        <td><%= delivery.length %></td>
        <td><%= delivery.width %></td>
        <td><%= delivery.height %></td>
        <td><%= delivery.common_volume %></td>
        <td><%= delivery.common_weight %></td>
        <td><%= delivery.distance %></td>
        <td><%= delivery.calculation%></td>
        <td><%= link_to 'Показать', delivery %></td>
        <td><%= link_to 'Редактировать', edit_delivery_path(delivery) %></td>
        <td><%= link_to 'Удалить', delivery, method: :delete, data: { confirm: 'Вы уверены?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%=link_to 'Новая доставка', root_path%>

<p>Топ-3 города отправки по популярности: 
<%=Delivery.group("from").order('count(*) DESC').limit(3).pluck("from").join(', ')%>.
</p>
